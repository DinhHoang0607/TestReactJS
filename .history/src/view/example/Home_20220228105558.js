import React from "react";
import { useHistory } from "react-router";
import { useHistory } from "react-router";
const Home = (props) => {
  console.log(props);
  return <div>Home</div>;
};

export default withRouter(Home);
