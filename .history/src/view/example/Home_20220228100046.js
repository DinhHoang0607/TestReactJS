import React from "react";
import { withRouter } from "react-router";
import { withRouter } from "react-router";
const Home = (props) => {
  console.log(props);
  return <div>Home</div>;
};

export default withRouter(Home);
