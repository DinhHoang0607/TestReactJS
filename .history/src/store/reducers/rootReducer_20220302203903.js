import React from 'react'

const initState = {
    users: [
        {id:1, name:'Hoang'}
        {id:1, name:'Vip'}
    ]
}
const rootReducer = ( st) => {
  return (
    <div>rootReducer</div>
  )
}

export default rootReducer